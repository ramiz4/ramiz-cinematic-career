const links = [['universe','Universe'],['career','Career'],['projects','Projects'],['architect','Architect'],['contact','Contact']];
export function Navigation() { return <nav className="nav" aria-label="Primary"><a className="brand" href="#top">RL<span>.</span></a><div className="nav__links">{links.map(([id,label]) => <a key={id} href={`#${id}`}>{label}</a>)}</div><a className="nav__cta" href="mailto:me@ramizloki.com">Contact</a></nav>; }
